/**
 * Returns an array of cities
 * Will updated to allow filtering by text
 * @param {array of objects} cities 
 */
export default function selectCities(cities) {
    return cities.map((city) => {
        return city;
    });
};